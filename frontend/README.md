# Option-Implied PDF Visualizer - React Frontend

Premium React + Tailwind + Framer Motion frontend.

## Quick Start

```bash
npm install
npm run dev
```

Frontend: http://localhost:5173
Backend: Start with `cd ../backend && python -m uvicorn api.main:app --reload`
